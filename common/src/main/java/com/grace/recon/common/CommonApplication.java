package com.grace.recon.common;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main entry point for the Reconciliation Engine Common Libraries Spring Boot application. This
 * class serves as a placeholder for common application-level configurations and can be used to run
 * the common module as a standalone Spring Boot application for testing or development purposes.
 */
@SpringBootApplication
public class CommonApplication {

  public static void main(String[] args) {
    SpringApplication.run(CommonApplication.class, args);
  }
}
